SYNC_SECRET=my_secure_password_123
PYTHON_VERSION=3.9.0
# DATABASE_URL=sqlite:///./cloud.db 
# Remove # above if you want to use SQLite file instead of Render's internal DB (not recommended for persistent data on free tier)
